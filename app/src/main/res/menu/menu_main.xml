<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.mynewsfeed.UIcontroller.MainActivity">
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" />
    <item android:id="@+id/ignored_source_menuItem"
        android:icon="@drawable/menu_banned"
        app:showAsAction="always"
        android:title="@string/menu_banned"/>
    <item android:id="@+id/refresh_menu"
        android:icon="@drawable/menu_refresh"
        app:showAsAction="always"
        android:title="@string/menu_refresh"/>

</menu>
